{
  "name": "@stackd/solana",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Solana helpers: xStock brand config, Token-2022 balance reads, SPL transfer utils.",
  "//type": "The source is ESM. Without this, Node/tsx treats these .ts files as CommonJS and named imports from apps/api fail with 'does not provide an export named BRANDS'. Next compiles the source itself via transpilePackages, so the web app is unaffected.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.95.4",
    "bs58": "^6.0.0"
  },
  "scripts": {
    "test": "node --import tsx --test src/rewards.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^20.16.11"
  }
}
